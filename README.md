## Build Setup

```bash
# install dependencies
$ composer install
$ npm install && npm run dev

# Database Migrate
$ php artisan migrate:fresh

# Database Seed
$ php artisan db:seed

# serve at localhost:8000
$ php artisan serve
```
